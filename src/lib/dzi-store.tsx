import { createContext,useContext,useState,type ReactNode } from "react";
import { initialOrders,initialProducts,type Order,type Product,type Role } from "./dzi-data";
type CartItem={product:Product;quantity:number};
type Store={role:Role;setRole:(r:Role)=>void;cart:CartItem[];cartOpen:boolean;setCartOpen:(v:boolean)=>void;add:(p:Product)=>void;remove:(id:string)=>void;quantity:(id:string,n:number)=>void;products:Product[];setProducts:React.Dispatch<React.SetStateAction<Product[]>>;orders:Order[];setOrders:React.Dispatch<React.SetStateAction<Order[]>>};
const C=createContext<Store|undefined>(undefined);
export function DziProvider({children}:{children:ReactNode}){const[role,setRole]=useState<Role>("Cliente");const[cart,setCart]=useState<CartItem[]>([]);const[cartOpen,setCartOpen]=useState(false);const[products,setProducts]=useState(initialProducts);const[orders,setOrders]=useState(initialOrders);const add=(product:Product)=>{setCart(c=>{const old=c.find(x=>x.product.id===product.id);return old?c.map(x=>x.product.id===product.id?{...x,quantity:x.quantity+1}:x):[...c,{product,quantity:1}]});setCartOpen(true)};const remove=(id:string)=>setCart(c=>c.filter(x=>x.product.id!==id));const quantity=(id:string,n:number)=>setCart(c=>n<1?c.filter(x=>x.product.id!==id):c.map(x=>x.product.id===id?{...x,quantity:n}:x));return <C.Provider value={{role,setRole,cart,cartOpen,setCartOpen,add,remove,quantity,products,setProducts,orders,setOrders}}>{children}</C.Provider>};export function useDzi(){const v=useContext(C);if(!v)throw new Error("DziProvider missing");return v}
