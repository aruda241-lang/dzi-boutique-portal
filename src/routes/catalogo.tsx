import{createFileRoute}from"@tanstack/react-router";import{Search,SlidersHorizontal}from"lucide-react";import{useMemo,useState}from"react";import{ProductCard}from"@/components/product-card";import{useDzi}from"@/lib/dzi-store";
export const Route=createFileRoute("/catalogo")({head:()=>({meta:[{title:"Catálogo — DZI"},{name:"description",content:"Explora abrigos, capas y accesorios DZI creados con fibras nobles."},{property:"og:title",content:"Catálogo — DZI"},{property:"og:description",content:"Piezas atemporales de lujo consciente."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Catalog});function Catalog(){const{products}=useDzi();const[q,setQ]=useState("");const[cat,setCat]=useState("Todas");const[stock,setStock]=useState(false);const filtered=useMemo(()=>products.filter(p=>(cat==="Todas"||p.category===cat)&&(!stock||p.stock>0)&&p.name.toLowerCase().includes(q.toLowerCase())),[products,q,cat,stock]);return <div className="page-shell"><p className="eyebrow">Colección completa</p><h1 className="page-title">Catálogo</h1><div className="mt-10 flex flex-col gap-4 border-y border-border py-5 md:flex-row md:items-center"><label className="flex flex-1 items-center gap-3"><Search className="size-4"/><input value={q} onChange={e=>setQ(e.target.value)} className="w-full bg-transparent outline-none" placeholder="Buscar una pieza"/></label><div className="flex items-center gap-3 text-sm"><SlidersHorizontal className="size-4"/><select value={cat} onChange={e=>setCat(e.target.value)} className="control"><option>Todas</option><option>Abrigos</option><option>Chaquetas</option><option>Accesorios</option><option>Prendas</option></select><label className="flex items-center gap-2"><input type="checkbox" checked={stock} onChange={e=>setStock(e.target.checked)}/> Disponible</label></div></div><p className="my-8 text-sm text-muted-foreground">{filtered.length} piezas</p><div className="grid gap-x-7 gap-y-12 sm:grid-cols-2 lg:grid-cols-3">{filtered.map(p=><ProductCard key={p.id} p={p}/>)}</div></div>}
