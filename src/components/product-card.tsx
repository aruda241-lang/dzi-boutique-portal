import { Link } from "@tanstack/react-router";import { ShoppingBag } from "lucide-react";import hero from "@/assets/dzi-hero.jpg";import materials from "@/assets/dzi-materials.jpg";import { Button } from "@/components/ui/button";import { useDzi } from "@/lib/dzi-store";import { money,type Product } from "@/lib/dzi-data";
export function ProductCard({p}:{p:Product}){const{add}=useDzi();const img=p.image==="hero"?hero:materials;return <article className="group"><Link to="/producto/$productId" params={{productId:p.id}} className="block aspect-[4/5] overflow-hidden bg-card"><img src={img} alt={p.name} width={640} height={800} loading="lazy" className="h-full w-full object-cover transition duration-700 group-hover:scale-[1.03]"/></Link><div className="mt-4 flex items-start gap-3"><div className="flex-1"><Link to="/producto/$productId" params={{productId:p.id}} className="font-display text-2xl">{p.name}</Link><p className="mt-1 text-sm text-muted-foreground">{money(p.price)} · {p.stock} disponibles</p></div><Button size="icon" variant="outline" onClick={()=>add(p)} aria-label={`Añadir ${p.name} al carrito`}><ShoppingBag/></Button></div></article>}
